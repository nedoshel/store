<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Store" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Store">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/favicon.png" type="image/png">
    <link rel="apple-touch-icon" href="/favicon.png">
    <%= favicon_link_tag '/favicon.png', rel: 'apple-touch-icon', type: 'image/png' %>
    <%= stylesheet_link_tag :flowbite %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="p-5 bg-white dark:bg-gray-900 antialiased text-white">   
    <% if notice %>
      <div id="notice" class="flex items-center p-4 mb-4 text-green-800 rounded-lg bg-blue-50 dark:bg-gray-800 dark:text-green-400" role="alert">
        <span class="font-medium"><%= alert %></span>
          <%= notice %>
        </span>
        <button type="button" data-dismiss-target="#notice" aria-label="Close" class="ms-auto -mx-1.5 -my-1.5 bg-blue-50 text-green-500 rounded-lg focus:ring-2 focus:ring-blue-400 p-1.5 hover:bg-blue-200 inline-flex items-center justify-center h-8 w-8 dark:bg-gray-800 dark:text-green-400 dark:hover:bg-gray-700">
            <span class="sr-only">Close</span>
            <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
            </svg>
        </button>
      </div>
    <% end %>
    <% if alert %>
      <div id="alert" class="flex items-center p-4 mb-4 text-red-800 rounded-lg bg-blue-50 dark:bg-gray-800 dark:text-red-400" role="alert">
        <span class="font-medium">
          <%= alert %>
        </span>
        <button type="button" class="ms-auto -mx-1.5 -my-1.5 bg-blue-50 text-red-500 rounded-lg focus:ring-2 focus:ring-blue-400 p-1.5 hover:bg-blue-200 inline-flex items-center justify-center h-8 w-8 dark:bg-gray-800 dark:text-red-400 dark:hover:bg-gray-700" data-dismiss-target="#alert" aria-label="Close" data-turbo="false">
            <span class="sr-only">Close</span>
            <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
            </svg>
        </button>
      </div>
    <% end %>

    <nav class="bg-white border-gray-200 dark:bg-gray-900">
        <div class="flex flex-wrap justify-between items-center mx-auto max-w-screen-xl p-4">
            <a href="<%= root_path %>" class="flex items-center space-x-3 rtl:space-x-reverse">
                <%= image_tag("/favicon.png", class: "h-8", alt: "Store Logo") %>
                <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white"><%= t("root.title") %></span>
            </a>
            <div class="flex items-center space-x-6 rtl:space-x-reverse">
                <nav class="navbar">
                  <div class="hidden w-full md:block md:w-auto" id="navbar-default">
                    <ul class="<%= navbar_ul_class %>">
                      <li>
                        <%= navbar_link_to t("root.home"), root_path %>
                      </li>

                      <% if authenticated? %>
                        <li>
                          <%= navbar_link_to t("settings.profiles.show.title"), settings_profile_path %>
                        </li>
                        <li>
                          <%= navbar_button_to t("sessions.destroy.title"), session_path, method: :delete %>
                        </li>
                      <% else %>
                        <li>
                          <%= navbar_link_to t("sign_ups.show.title"), sign_up_path %>
                        </li>
                        <li>
                          <%= navbar_link_to t("sessions.new.title"), new_session_path %>
                        </li>
                      <% end %>
                    </ul>
                  </div>
                </nav>
            </div>
        </div>
    </nav>

    <main>
      <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
  </body>
</html>
